'use strict';

let fs = require('fs');
let log_path = './logs/';
let log_name = 'log'; // will extend name with each log file written
let moment = require('moment');

// provide a fcn that accepts log events
function log(){


}
// write log events with details to a file

// TODO: a log reader?

module.exports.log = log;